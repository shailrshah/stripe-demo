{
  "name": "stripe-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "test:e2e": "node --test --test-concurrency=1 'e2e/**/*.e2e.js'"
  },
  "dependencies": {
    "dotenv": "^18.0.3",
    "express": "^5.2.1",
    "stripe": "^22.6.2"
  }
}
